[project]
name = "daemon"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "MattiaAlessi", email = "@users.noreply.github.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
daemon = "daemon:main"

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"
